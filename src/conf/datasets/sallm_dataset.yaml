dataset_preparation:
  paths:
    source_directory: "data/filtered_data"
    output_directory: "data/sallm_dataset"

  splits:
    validation_size: 0.1
    test_size: 0.1

    capping:
      max_tokens: 2000000
      chars_per_token_estimate: 4

  settings:
    seed: 42
