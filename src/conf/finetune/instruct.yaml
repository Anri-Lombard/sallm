architecture: ???
language: ???

defaults:
  - /architecture: ${architecture}
  - /dataset: instruct/${language}
  - /training: instruct
  - /tokenizer: base
  - _self_

mode: FINETUNE
wandb:
  project: "sallm-ft"
  name: "${architecture}-instruct-${language}"

training:
  output_dir: "${oc.env:SCRATCH}/masters/sallm/checkpoints/ft_${architecture}_instruct_${language}"
  logging_dir: "${oc.env:SCRATCH}/masters/sallm/logs/ft_${architecture}_instruct_${language}"
  run_name: "${architecture}-instruct-${language}"
