architecture: ???
language: ???

defaults:
  - architecture: ${architecture}
  - dataset: news/${language}
  - training: news
  - tokenizers: base
  - _self_

mode: FINETUNE
wandb:
  project: "sallm-ft"
  name: "${architecture}-news-${language}"

training:
  output_dir: "${oc.env:SCRATCH}/masters/sallm/checkpoints/ft_${architecture}_news_${language}"
  logging_dir: "${oc.env:SCRATCH}/masters/sallm/logs/ft_${architecture}_news_${language}"
  run_name: "${architecture}-news-${language}"
