defaults:
  - _self_
  - model: llama/125m
  - dataset: masakhanews
  - finetune/_defaults

mode: FINETUNE

training:
  lang: "eng"
  dataset: "masakhanews"

wandb:
  project: "sallm-ft"
  name: "ft-${training.model_id}-${training.dataset}-${training.lang}"
