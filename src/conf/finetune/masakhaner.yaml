defaults:
  - _self_
  - model: llama/125m
  - dataset: masakhaner
  - finetune/_defaults

mode: FINETUNE

training:
  lang: "xho"
  dataset: "masakhaner"

wandb:
  project: "sallm-ft"
  name: "ft-${training.model_id}-${training.dataset}-${training.lang}"
