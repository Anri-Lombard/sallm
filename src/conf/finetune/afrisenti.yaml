defaults:
  - _self_
  - model: llama/125m
  - dataset: afrisenti
  - finetune/_defaults

mode: FINETUNE

training:
  lang: "tso"
  dataset: "afrisenti"

wandb:
  project: "sallm-ft"
  name: "ft-${training.model_id}-${training.dataset}-${training.lang}"
