architecture: ???
language: ???

defaults:
  - /architecture: ${architecture}
  - /dataset: injongointent/${language}
  - /training: injongointent
  - /tokenizer: base
  - _self_

mode: FINETUNE
wandb:
  project: "sallm-ft"
  name: "${architecture}-injongo-intent-${language}"

training:
  output_dir: "${oc.env:SCRATCH}/masters/sallm/checkpoints/ft_${architecture}_injongointent_${language}"
  logging_dir: "${oc.env:SCRATCH}/masters/sallm/logs/ft_${architecture}_injongointent_${language}"
  run_name: "${architecture}-injongo-intent-${language}"
